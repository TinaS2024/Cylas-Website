datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Gast {
  id String @id @default(uuid())
  name String @unique
  nachricht String
}
