datasource db {
  provider = "postgresql" //sqlite
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "/app/generated/prisma-client"
}

model Gast {
  id                Int @id @default(autoincrement())
  erstellungsdatum  DateTime @default (now()) 
  name              String @unique
  nachricht         String
}

// npx prisma ... zeigt Befehle die möglich sind
// prisma migrate dev : init  ... erstellt die Tabelle
// prisma studio ... öffnet die Datenbank im Browser bei port 5555